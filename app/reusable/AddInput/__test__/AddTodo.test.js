import React from 'react';
import {render, screen, fireEvent} from '@testing-library/react-native';
// import AddTodo from './AddTodo';

// mocking a function

// const mockedFn = jest.fn();

// we'll be using describe block to group all our test into one component
describe('AddInput', () => {
  // describe('functionality 1', () => {
  it('should be able to test the change in the input', async () => {
    // render(<AddTodo todo={[]} setTodos={mockedFn} />);
    // const inputElement = screen.getByPlaceholderText(/add a new task here/i);
    // expect(inputElement).toBeTruthy;
    // });
  });

  // determine if input field is working
  // it('should be able to type in the input field', async () => {
  //   render(<AddTodo todo={[]} setTodos={mockedFn} />);
  //   const inputElement = screen.getByPlaceholderText(/add a new task here/i);
  //   fireEvent.changeText(inputElement);
  //   expect(inputElement).toBeCalled;
  // });
});
